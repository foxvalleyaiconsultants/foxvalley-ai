# ============================================================
# .env — Create this file as ".env" in the project root
# Fill in your actual values — NEVER commit this file to git
# ============================================================

# MySQL database connection string
# Format: mysql://username:password@host:port/database_name
DATABASE_URL=mysql://foxvalley_user:YOUR_STRONG_PASSWORD@localhost:3306/foxvalley_ai

# Secret key for signing JWT session tokens
# Generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=PASTE_YOUR_GENERATED_SECRET_HERE

# Server port (your reverse proxy will forward to this)
PORT=3000

# Node environment
NODE_ENV=production

